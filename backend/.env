# The local port configuration your Express API server will listen on
PORT=5001

# Connection URI string targeting your local MongoDB instance
MONGO_URI=mongodb://localhost:27017/trim

# The base address location used by the backend to construct full short URL paths
BASE_URL=http://localhost:5001